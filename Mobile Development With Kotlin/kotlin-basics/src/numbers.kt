fun main(){
//    in kotlin, there are 4 types of numbers i.e, int, float, long, double

    val i = 2;
    val l = 2L;

//    both floats and double store decimal numbers but double has higher precision then float because it takes double memory as an int

    val f = 2F;
    val d = 2.0;

    val a = 5;
    val b = 2;

    println(a / b) // it answers in int division
    println(a.toDouble() / b) // it answers in float division

    println(50+2*3)
}