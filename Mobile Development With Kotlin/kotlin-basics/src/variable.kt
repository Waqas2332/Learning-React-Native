fun main(){

//    In Kotlin, values are inferred from the initial value which is assign to it.
    val name = "Leonard"
    val age = "12"

    println("Name is $name & age is $age")


//    In kotlin, variables are declared by using var keyword and values ( variables whose values can't be changed like constants are declared by used val )
}